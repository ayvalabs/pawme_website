rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Public read access for reward images
    match /rewards/{allPaths=**} {
      allow read;
      // Allow write only for authenticated users (admin will be authenticated)
      allow write: if request.auth != null;
    }
  }
}
